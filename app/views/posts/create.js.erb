wrapper = $('.post-wrapper')
wrapper.find('>ul').prepend("<%= j render 'post', post: @post %>")
creator = wrapper.find('.post-form')
textarea = creator.find('textarea')
textarea.val('')
shadow = creator.find('.shadow')
shadow.html('')
textarea.height(shadow.height())
